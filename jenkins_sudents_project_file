pipeline{
   agent {
     label "slave-1-labels"
   }
stages {
  stage ("git clone"){
    steps{
            git branch: 'main', https://github.com/Ranjankumarjena-rj/students-project.git'
      }
    }
 stage  ("maven package") {
   steps {
    sh 'mvn clean package'
            }
 }
}
}
