pipeline{
   agent {
     label "slave-1-labels"
   }
stages {
  stage ("git clone"){
    steps{
            git branch: 'main', url: 'https://github.com/Ranjankumarjena-rj/students-project.git'
      }
    }
 stage  ("maven package") {
   steps {
    sh 'mvn clean package'
            }
          }
  stage ("tomcat deploy") {
    steps{
       sh 'cp /home/ubuntu/workspace/student-service/target /var/lib/tomat10/webapps/ .
         }
      }
   }
}
