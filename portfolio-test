pipeline{
   agent {
     label "portfolio-labels"
   }
stages {
  stage ("git clone"){
    steps{
            git branch: 'main', url: 'https://github.com/Ranjankumarjena-rj/portfolio-test.git'
      }
    }
 stage  ("maven package") {
   steps {
    sh 'mvn clean package'
            }
          }
  stage ("tomcat deploy") {
    steps{
       sh 'sudo cp /home/ubuntu/workspace/portfolio-test/target/portfolio.war /var/lib/tomcat10/webapps '
         }
      }
   }
}
